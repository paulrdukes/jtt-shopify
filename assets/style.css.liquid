/*
 * Just The Text
 * A Shopify theme.
 *
 * Last update: Dec. 8 2012
 *
 * @version     0.5
 * @author      Paul Dukes @paulrdukes
 * @github      https://github.com/paulrdukes/jtt-shopify
 *
 * Built with
 * HTML5 Boilerplate - h5bp.com
 * Lettering.js - letteringjs.com/
 *
 */

/* ! normalize.css v1.0.1 | MIT License | git.io/normalize */
article, aside, details, figcaption, figure, footer, header, hgroup, nav, section, summary { display: block; }
audio, canvas, video { display: inline-block;  *display: inline; *zoom: 1; }
audio:not([controls]) { display: none; height: 0; }
[hidden] { display: none; }
html { font-size: 100%; -webkit-text-size-adjust: 100%; -ms-text-size-adjust: 100%; }
html, button, input, select, textarea { font-family: sans-serif; }
body { margin: 0; }
a:focus { outline: thin dotted; }
a:active, a:hover { outline: 0; }
h1 { font-size: 2em; margin: 0.67em 0; }
h2 { font-size: 1.5em; margin: 0.83em 0; }
h3 { font-size: 1.17em; margin: 1em 0; }
h4 { font-size: 1em; margin: 1.33em 0; }
h5 { font-size: 0.83em; margin: 1.67em 0; }
h6 { font-size: 0.75em; margin: 2.33em 0; }
abbr[title] { border-bottom: 1px dotted; }
b, strong { font-weight: bold; }
blockquote { margin: 1em 40px; }
dfn { font-style: italic; }
mark { background: #ff0; color: #000; }
p, pre { margin: 1em 0; }
code, kbd, pre, samp { font-family: monospace, serif; _font-family: 'courier new', monospace; font-size: 1em; }
pre { white-space: pre; white-space: pre-wrap; word-wrap: break-word; }
q { quotes: none; }
q:before, q:after { content: ''; content: none; }
small { font-size: 80%; }
sub, sup { font-size: 75%; line-height: 0; position: relative; vertical-align: baseline; }
sup { top: -0.5em; }
sub { bottom: -0.25em; }
dl, menu, ol, ul { margin: 1em 0; }
dd { margin: 0 0 0 40px; }
menu, ol, ul { padding: 0; } /* edit - originally 0 0 0 40px */
nav ul, nav ol { list-style: none; list-style-image: none; }
img { border: 0; -ms-interpolation-mode: bicubic; }
svg:not(:root) { overflow: hidden; }
figure { margin: 0; }
form { margin: 0; }
fieldset { border: 1px solid #c0c0c0; margin: 0 2px; padding: 0.35em 0.625em 0.75em; }
legend { border: 0; padding: 0; white-space: normal; *margin-left: -7px; }
button, input, select, textarea { font-size: 100%; margin: 0; vertical-align: baseline; *vertical-align: middle; }
button, input { line-height: normal; }
button, html input[type="button"], input[type="reset"], input[type="submit"] { -webkit-appearance: button; cursor: pointer; *overflow: visible; }
button[disabled], input[disabled] { cursor: default; }
input[type="checkbox"], input[type="radio"] { box-sizing: border-box; padding: 0; *height: 13px; *width: 13px; }
input[type="search"] { -webkit-appearance: textfield; -moz-box-sizing: content-box; -webkit-box-sizing: content-box; box-sizing: content-box; }
input[type="search"]::-webkit-search-cancel-button, input[type="search"]::-webkit-search-decoration { -webkit-appearance: none; }
button::-moz-focus-inner, input::-moz-focus-inner { border: 0; padding: 0; }
textarea { overflow: auto; vertical-align: top; }
table { border-collapse: collapse; border-spacing: 0; }


/*
 * Fonts
 * All fonts available at http://www.fontsquirrel.com
 */

/*
 * League Gothic Regular
 *
 * This Font Software is licensed under the SIL Open Font License, Version 1.1.
 * This license is copied below, and is also available with a FAQ at:
 * http://scripts.sil.org/OFL
 */
 @font-face {
    font-family: 'LeagueGothicRegular';
    src: url('League_Gothic-webfont.eot');
    src: url('League_Gothic-webfont.eot?#iefix') format('embedded-opentype'),
         url('League_Gothic-webfont.woff') format('woff'),
         url('League_Gothic-webfont.ttf') format('truetype'),
         url('League_Gothic-webfont.svg#LeagueGothicRegular') format('svg');
    font-weight: normal;
    font-style: normal;
}

/*
 * Bebas Regular
 *
 * All fonts copyright Flat-it(http://flat-it.com/)
 * This is a freeware typeface.
 */

@font-face {
    font-family: 'BebasRegular';
    src: url('BEBAS___-webfont.eot');
    src: url('BEBAS___-webfont.eot?#iefix') format('embedded-opentype'),
         url('BEBAS___-webfont.woff') format('woff'),
         url('BEBAS___-webfont.ttf') format('truetype'),
         url('BEBAS___-webfont.svg#BebasRegular') format('svg');
    font-weight: normal;
    font-style: normal;
}

/*
 * Icon Fonts
 */
@font-face {
  font-family: "SocialFoundicons";
  src: url("social_foundicons.eot");
  src: url("social_foundicons.eot?#iefix") format("embedded-opentype"),
       url("social_foundicons.woff") format("woff"),
       url("social_foundicons.ttf") format("truetype"),
       url("social_foundicons.svg#SocialFoundicons") format("svg");
  font-weight: normal;
  font-style: normal;
}

/*
 * Socialfoundicon styles
 */
.icon {
  font-size: 2em;
  display: inline;
  width: auto;
  height: auto;
  line-height: inherit;
  vertical-align: baseline;
  background-image: none;
  background-position: 0 0;
  background-repeat: repeat;
}

.icon:before {
  font-family: "SocialFoundicons";
  font-weight: normal;
  font-style: normal;
  text-decoration: inherit;
  content: attr(data-icon);
}

/*
 * HTML5 Boilerplate styles
 */

html, button, input, select, textarea { color: {{ settings.body_text_color }}; }
body {
    font: 1em/1.4 {{ settings.body_font }};
    background-color: {{ settings.background_color }} ;
    background-image: url('{{"6.png" | asset_url}}'); /* add background image to settings */
    background-repeat: repeat; /* add background repeat to settings */


}

::-moz-selection { background: #b3d4fc; text-shadow: none; }
::selection { background: #b3d4fc; text-shadow: none; }

hr {
    display: block;
    height: 1px;
    border: 0;
    border-top: 1px dotted {{ settings.nav_text_color }};
    margin: 1em 0;
    padding: 0;
}

img { vertical-align: middle; }
fieldset {
    border: 0;
    margin: 0;
    padding: 0;
}

textarea { resize: vertical; }

.chromeframe {
    margin: 0.2em 0;
    background: #ccc;
    color: #000;
    padding: 0.2em 0;
}


/*
 * General Selectors
 */
.wrapper{
    width:960px;
    margin:25px auto;
}
.fancy{}
.word2{ font-size: .25em !important; margin: -25px;}
#banner-wrapper{
    width:100%;
    position: fixed;
    top:0;
    left:0;
    background-color: {{ settings.banner_background_color }};
    font-size: 0.8em;
    z-index: 10;

}
#banner{
    width: 960px;
    margin: 0 auto;
}

#logo-banner{}
#aside{ width: 240px; position: fixed;}
    #nav {}
        #nav ul{ width:100%; padding:0; }
        #nav ul li{ list-style: none; margin:0; }
        #nav a{
            display: block;
            padding: 5px 10px 5px 0;
            margin-left: 10px;
            text-decoration: none;
            font-family: {{ settings.nav_font }};
            background: {{settings.nav_background_color }};
        }
        #nav a:hover {
            background: {{ settings.nav_background_hover_color }};
            {% if settings.nav_hover_border %}
                border-left: 10px solid {{settings.nav_text_hover_color}};
                margin-left: -10px;
                padding-left: 10px;
            {% endif %}
        }

    #cart, #login, #search { margin:5px; padding:5px 10px 2px;}
    #login{margin:5px 0;}
    #powered-by{font-size: 0.8em;}
    #powered-by a{ color: {{ settings.header_text_color }};}
    #cart {border-left: 1px solid {{ settings.body_text_color }}; }

        #search form{  }
        #search input[type="text"],#search input[type="text"]:focus{
            border:none;
        }
        input[type="submit"]{
            border-bottom: 1px solid {{settings.nav_text_color}};
            color:{{ settings.nav_text_color}};
        }
        input[type="submit"]:hover{
            color:{{ settings.nav_text_hover_color }};

        }
    #social {margin:0;}
    #social ul li{list-style: none; display: inline-block;}

    h1 a, h2 a, h3 a, h4 a, h5 a, h6 a, #social a, .title a {text-decoration: none; }
    a{color:{{ settings.nav_text_color }}; }
    a:hover, #powered-by a:hover{ color:{{ settings.nav_text_hover_color }}; }


#tags li{ display: inline; list-style: none;}
.active, .current-tag{background-color: {{ settings.body_text_color }};}

.button{}


#main{
    width:620px;

    margin: 0 0 20px 20px;

    float: right;
    height: 300px;
}

    .static ul, .blog-content ul{margin: 0 0 0 40px;}

.title{
    margin:0;
    padding:0;
    font-size: {{ settings.logo_size}};
    color: {{ settings.logo_color }};
    border-bottom: 2px solid {{ settings.body_text_color }};
    letter-spacing: 0.03em;
}
.sub-title{
    opacity: 0.75;
    font-family: {{settings.sub_title_font }};
    font-size: {{ settings.sub_title_size}};
}

h1,h2,h3,h4,h5,h6{
    color: {{ settings.header_text_color }};
    font-family: {{settings.logo_font}};
    text-transform: {{settings.logo_case}};
}

/*
 * Product Selectors
 */
 #product-loop{}
    #product-loop {list-style:none; }
    #product-loop li{
        display: inline-block;
        margin-bottom: 10px;
        padding:0 10px;
        width:160px;
    }
    #product-loop .title{
        border: none;
        font-family:{{ settings.logo_font}};
        font-size: 0.9em;}
.product{  margin: 0; padding: 0;}

.product-header{ border-bottom:2px solid {{ settings.body_text_color }}; font-family:{{settings.logo_font}}; }

    .product-title h3{ padding: 0; margin: 0; }
    .product-price { }

.product-wrapper{margin-top:10px;}
    .product-image{
        border:1px solid {{ settings.body_text_color }};
        margin: 0 10px 0 0;
    }
.product-price sup {text-decoration: underline; top: -0.2em;}
/*
 * Cart Selectors
 */
.cart-thumb{}
.cart-title, .cart-unitprice, .cart-quantity, .cart-total{margin-right: 20px;}
.cart-remove{}
.cart-item{}
.update{}
.checkout{}

tr:nth-of-type(2n){background-color: #aaa;}

/*
 * Media Queries

@media screen and (max-width: 480px){
    #aside, #main, #footer{min-width: 100%; margin: 0; padding:0; box-shadow: 0 0 0;}
    #aside{position: relative;}
    #social, #aside #powered-by{ display: none !important; visibility: hidden; }
    #footer { visibility: visible !important; display: block; }
}
 */
/*
 * Helper classes
 */
.ir {background-color: transparent; border: 0; overflow: hidden; *text-indent: -9999px; }
.ir:before { content: ""; display: block; width: 0; height: 100%; }
.hidden { display: none !important; visibility: hidden; }
.visuallyhidden { border: 0; clip: rect(0 0 0 0); height: 1px; margin: -1px; overflow: hidden; padding: 0; position: absolute; width: 1px; }
.visuallyhidden.focusable:active, .visuallyhidden.focusable:focus { clip: auto; height: auto; margin: 0; overflow: visible; position: static; width: auto; }
.invisible { visibility: hidden; }
.clearfix:before, .clearfix:after { content: " "; display: table; }
.clearfix:after { clear: both; }
.clearfix { *zoom: 1; }
.left{ float:left; }
.right{ float: right; }

/*
 * Print styles
 */
@media print {
    * { background: transparent !important; color: #000 !important; box-shadow: none !important; text-shadow: none !important; }
    a, a:visited { text-decoration: underline; }
    a[href]:after {  content: " (" attr(href) ")"; }
    abbr[title]:after {  content: " (" attr(title) ")"; }

    /*
     * Don't show links for images, or javascript/internal links
     */
    .ir a:after, a[href^="javascript:"]:after, a[href^="#"]:after { content: ""; }
    pre, blockquote { border: 1px solid #999; page-break-inside: avoid; }
    thead {  display: table-header-group; }
    tr, img { page-break-inside: avoid; }
    img {  max-width: 100% !important; }
    @page {  margin: 0.5cm; }
    p, h2, h3 { orphans: 3; widows: 3; }
    h2, h3 { page-break-after: avoid; }
}